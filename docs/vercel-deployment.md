<div dir="rtl">

# ربط Vercel — سجل ومَحجوب

**Dalal — منصة المزادات المباشرة**

| الحقل | القيمة |
|---|---|
| المعرّف | **S0-05** |
| المالك في الخطة | عبدالرحمن — [`@Dem4t`](https://github.com/Dem4t) |
| **المالك المقترح** | **ريان — [`@RayanAlDwlah`](https://github.com/RayanAlDwlah)** — راجع §5 |
| الحالة | ✅ **الربط تمّ** · ✅ **النشر ينجح** (2026-08-13) — ⚠️ يبقى بندان. راجع §11 |
| التاريخ | 2026-08-12 · **مُحدَّثة 2026-08-13** |
| المرجع المعماري | [`ARCHITECTURE.md`](../ARCHITECTURE.md) §18 · §18.4 |

> **هذه الوثيقة رسالة جاهزة لريان.** تشرح ما جُرِّب، وما حدث، ولماذا، وما المطلوب منه بالضبط.
>
> **§1 → §10 محفوظة كسجل لما حدث يوم 2026-08-12.** الحاجز الذي تصفه **انفكّ** — ريان أنشأ المشروع كما طُلب في §6. الحالة الجارية في **§11**.

---

## 1. الخلاصة في سطرين

ربط Vercel بالمستودع **يتطلب تصريح وصول من مالك المستودع**. المستودع يخص **ريان**، وتطبيق Vercel على GitHub مثبَّت في نطاق **عبدالرحمن**. فلا يظهر المستودع في قائمة الاستيراد أصلًا.

**المطلوب: خطوة واحدة من ريان.** التفاصيل في §6.

---

## 2. ⚠️ تحذير حرج — مصيدة الاستنساخ

> ### إذا أدخلت رابط المستودع يدويًا في Vercel، **لا تضغط `Create`**

عند إدخال `https://github.com/RayanAlDwlah/dallal` في حقل Vercel، **لا يظهر خطأ**. بل تُعرض شاشة تبدو سليمة:

```text
New Project
  Cloning from GitHub        RayanAlDwlah/dallal   main
  Git Scope: Dem4t   /   Private Repository Name: dallal
  [ Create ]
```

**هذا مسار Clone لا Import.** الضغط على `Create` يُنشئ **مستودعًا ثانيًا** باسم `Dem4t/dallal` كنسخة، ويربط النشر **بالنسخة لا بمستودع الفريق**.

| العطب | الأثر |
|---|---|
| مستودع ثانٍ | يخالف قرار «مستودع واحد مشترك» صراحةً |
| النشر من النسخة | محمد وريان يدفعون في مستودع، والنشر من مستودع آخر |
| انفصال الفريق | برانشات وPRs موزّعة على مستودعين |

**لم يُضغط `Create`. لم يُنشأ أي مستودع ولا أي مشروع Vercel.** الكلمة الوحيدة التي تفرّق بين الصحيح والكارثة هنا هي **`Cloning`** في أعلى الشاشة.

---

## 3. ما جُرِّب بالتفصيل

| # | المحاولة | النتيجة |
|---|---|---|
| 1 | `vercel.com/new` ← **Continue with GitHub** من متصفح مؤتمت | ❌ `The popup to connect GitHub could not be opened` — تفويض OAuth يحتاج نافذة منبثقة |
| 2 | عبدالرحمن ربط GitHub بنفسه من متصفحه | ✅ الربط نجح — النطاق `Dem4t` |
| 3 | البحث عن `dallal` في قائمة الاستيراد | ❌ غير موجود. الظاهر فقط: `temporary-workspace` · `BuildMindAI` — أي مستودعات `Dem4t` |
| 4 | تغيير النطاق في القائمة المنسدلة | ❌ لا يوجد نطاق `RayanAlDwlah` — لا يظهر إلا ما ثُبِّت عليه التطبيق |
| 5 | إدخال رابط المستودع يدويًا | ⚠️ عُرضت **مصيدة الاستنساخ** — §2. أُلغيت |

---

## 4. السبب الجذري

```text
تطبيق Vercel على GitHub مثبَّت في نطاق:   Dem4t          ← حساب عبدالرحمن
المستودع المطلوب يخص:                      RayanAlDwlah   ← حساب ريان
```

**Vercel لا يرى مستودعًا لا يملك تصريح وصول إليه.** وتثبيت GitHub App على حساب شخص آخر يتطلب إذنه.

**كون عبدالرحمن `collaborator` بصلاحية كتابة لا يكفي.** صلاحية الكتابة تسمح بالدفع والـPRs، لكن **تثبيت تطبيقات GitHub يتطلب `admin` على المستودع** — وهي عند مالكه.

> **هذا اعتماد تقني لم يكن ظاهرًا وقت التخطيط.** `GITHUB_PLAN.md` أسند `S0-05` لعبدالرحمن لتوزيع حمل Sprint 0 عن ريان — وهو سبب سليم، لكنه لم يأخذ في الحساب أن الربط يحتاج صلاحية على المستودع.

---

## 5. مقترح تعديل الملكية

| البند | المالك في الخطة | **المقترح** | السبب |
|---|---|---|---|
| **S0-05** ربط Vercel | عبدالرحمن | **ريان** | يملك المستودع، فالربط عنده يعمل بلا أذونات إضافية |
| **S0-06** متغيرات البيئة | عبدالرحمن | **عبدالرحمن** — دون تغيير | القيم تُضاف بعد وجود المشروع |
| **V-3** هدف المعاينات + إيميل الاستعادة | عبدالرحمن | **عبدالرحمن** — دون تغيير | يتحقق بعد الربط |

> **هذا ليس نقلًا لملكية نطاق.** ملكيات `TEAM.md` §3 · §4 · §5 كما هي: المصادقة لعبدالرحمن، المزادات لمحمد، المزايدة والريلتايم لريان. التعديل مقصور على **مَن يملك الصلاحية التقنية لخطوة إعداد واحدة**.

---

## 6. المطلوب من ريان — الخطوات

### الطريقة المفضّلة: ريان ينشئ مشروع Vercel

| # | الخطوة |
|---|---|
| 1 | سجّل الدخول إلى [vercel.com](https://vercel.com) بحساب ريان |
| 2 | `Add New` ← `Project` |
| 3 | `Continue with GitHub` ← وافق على التفويض |
| 4 | عند اختيار النطاق: **`Only select repositories`** واختر **`dallal` فقط** — لا تمنح وصولًا لكل المستودعات |
| 5 | اختر `RayanAlDwlah/dallal` ← **`Import`** — تأكد أنه `Import` **لا** `Clone` |
| 6 | طبّق الإعدادات في §7 |
| 7 | أضف متغيرات البيئة في §8 |

### البديل: ريان يمنح الوصول وعبدالرحمن يستورد

ريان يفتح `github.com/settings/installations` ← تطبيق Vercel ← يمنح الوصول إلى `dallal`. ثم يظهر المستودع لعبدالرحمن في Vercel.

**يحتاج خطوة من ريان في الحالتين**، فالطريقة الأولى أقصر.

---

## 7. إعدادات مشروع Vercel

| الإعداد | القيمة |
|---|---|
| Project Name | `dallal` |
| Framework Preset | **Next.js** — [`design/STACK.md`](../design/STACK.md) |
| Root Directory | `./` |
| Production Branch | **`main`** |
| Build / Output | افتراضي Next.js — لا تعديل |

**النتيجة المطلوبة** — `ARCHITECTURE.md` §18.2:

| المصدر | ينشر إلى |
|---|---|
| `main` | **الإنتاج** |
| أي Pull Request | **معاينة** برابط مستقل |

> **⚠️ أول بناء سيفشل** — لا يوجد `package.json` على `main` بعد. السكافولد (`S0-07`) لمحمد. **هذا متوقّع ومقبول**، ويصلح نفسه تلقائيًا عند اندماج السكافولد.

> **تصحيح 2026-08-13:** هذا التوقّع صحيح بالنسبة لـ `main`، **لكنه لا يفسّر الفشل الذي وقع فعلًا.** الفشل المرصود كان على **معاينة فرع محمد** — وهو فرع فيه `package.json` وبناؤه ينجح محليًا. السبب مختلف تمامًا؛ راجع §11.

**لا تُنشئ GitHub Actions** — تكامل Vercel نفسه يكفي (`GITHUB_PLAN.md` §11.2).

---

## 8. متغيرات البيئة

المصدر: [`docs/environment-contract.md`](environment-contract.md) · المشروعان: [`docs/supabase-projects.md`](supabase-projects.md)

| المتغيّر | Production | Preview + Development |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | رابط **`dallal-prod`** | رابط **`dallal-dev`** |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | مفتاح **`dallal-prod`** | مفتاح **`dallal-dev`** |

> ### ⚠️ المعاينات يجب أن تشير إلى `dallal-dev` — وأبدًا إلى الإنتاج
>
> `ARCHITECTURE.md` §18.4 يرفض الخيار C صراحةً. كود غير مراجَع في PR قد يكتب في بيانات الإنتاج.

**لا تُضف `SUPABASE_SERVICE_ROLE_KEY`.** التطبيق لا يحتاجه (`ARCHITECTURE.md` §17.3)، وظهوره **إشارة انحراف معماري تُناقَش قبل إضافتها** — `environment-contract.md` §3.

---

## 9. ملاحظة: خطة Hobby

حساب عبدالرحمن على Vercel خطة **Hobby**، وكذا المتوقع لريان. Hobby لا يدعم أعضاء فريق على المشروع.

| الأثر | التقييم |
|---|---|
| مالك المشروع وحده يرى اللوحة وسجلات البناء | ⚠️ مقبول — روابط المعاينة تعمل للجميع |
| المراجعون يفتحون رابط المعاينة من الـPR | ✅ يكفي لـ `CP-2` واختبار المتصفحين |
| ضبط متغيرات البيئة محصور بالمالك | ⚠️ يُنسّق معه |

**لا يمنع أي متطلب في `PRD.md`.** يُسجَّل كقيد معروف لا كعقبة.

---

## 10. ما يُغلق `S0-05`

- [ ] مشروع Vercel مرتبط بـ **`RayanAlDwlah/dallal`** — لا بنسخة ولا fork
- [ ] `main` ينشر إلى الإنتاج
- [ ] أي PR ينتج معاينة برابط مستقل
- [ ] الإنتاج يشير إلى `dallal-prod`
- [ ] **المعاينات تشير إلى `dallal-dev`** — لا الإنتاج
- [ ] لا `SUPABASE_SERVICE_ROLE_KEY` في الإعدادات
- [ ] لا GitHub Actions
- [ ] **لم يُنشأ أي مستودع ثانٍ**

ثم يُنفَّذ **`V-3`**: تأكيد هدف المعاينات، والتحقق من عمل إيميل استعادة كلمة المرور في المعاينة — وبدونه لا يمكن اختبار `US-23` قبل الدمج.

---

## 11. الحالة الجارية — 2026-08-13

> **§1 → §6 محفوظة كسجل لما حدث يوم 2026-08-12.** الحاجز الذي تصفه **انفكّ**؛ هذا القسم يصف ما بعده.

### 11.1 الربط تمّ ✅

| البند | القيمة |
|---|---|
| مشروع Vercel | `dallal` |
| النطاق | **`rayan-saleh1`** — أي أن ريان نفّذ الطريقة المفضّلة في §6 |
| معرّف المشروع | `prj_AVvQaQ6AHKMoEZYgPXiRpPPKwN9u` |
| هل نُشئ مستودع ثانٍ؟ | ❌ **لا** — مصيدة الاستنساخ (§2) تُجُنِّبت. النشر يعمل على `RayanAlDwlah/dallal` |

**اقتراح نقل ملكية `S0-05` إلى ريان (§5) نُفِّذ عمليًا.**

### 11.2 النشر كان يفشل — ثم أُصلح ✅

**قبل الإصلاح:**

| البند | القيمة |
|---|---|
| النشر الفاشل | `dpl_GsDPpG1cZXUpZxzZR85CPSydb1wW` |
| الفرع / اللقطة | `feature/mohammed-auctions` @ `48c6f91` |
| المدة | **~٢٠ ثانية** (`17:16:09Z` معلّق ← `17:16:29Z` فاشل) |
| رابط المعاينة | **فارغ** — لم يُنتَج |
| حالة الـ PR | `mergeable_state = unstable` بسبب فحص Vercel |

نصّ حالة GitHub وقتها:

```text
Deployment has failed — run this Vercel CLI command:
npx vercel inspect dpl_GsDPpG1cZXUpZxzZR85CPSydb1wW --logs
```

**بعد الإصلاح — 2026-08-13:**

| البند | القيمة |
|---|---|
| اللقطة | `feature/mohammed-auctions` @ `e246fb7` |
| حالة الفحص | ✅ **`success`** — "Deployment has completed" (`09:38:37Z`) |
| المدة | **١٦ ثانية** |
| رابط المعاينة | `https://dallal-cafxxfsso-rayan-saleh1.vercel.app` |
| حالة الـ PR | **`mergeable_state = clean`** — كل الفحوصات خضراء |

### 11.3 السبب — مُشخَّص ومُعاد إنتاجه، لا مُفترَض

**`next/font/google` يجلب الخطوط من `fonts.googleapis.com` أثناء `next build` نفسه.** `app/fonts.ts` يطلب `IBM Plex Sans Arabic` و`IBM Plex Sans`، فإذا عجز مضيف البناء عن الوصول، يسقط البناء كاملًا.

**كيف تحقّقنا:** بناء ناجح محليًا يكتب ملفات `.woff2` مجلوبة من `fonts.gstatic.com` داخل `.next/` — وهذا وحده يثبت وجود طلب شبكة أثناء البناء. ثم أُعيد البناء مع حجب المنفذ، فأنتج توقيع الفشل نفسه:

```text
> Build error occurred
Error: Turbopack build failed with 2 errors:
Error: next/font: error: Failed to fetch IBM Plex Sans from Google Fonts.
```

خروج `1`، ومدّة قصيرة تطابق الـ ٢٠ ثانية المرصودة.

> **قيد على هذا التشخيص:** **سجلّ البناء الخام من Vercel لم يُقرأ** — الحساب المتاح للجلسة لا يملك وصولًا إلى نطاق `rayan-saleh1`. التشخيص مبني على توقيع الخطأ المؤكَّد من صاحب الفرع + إعادة إنتاج مطابقة محليًا. إن أظهر السجل الحقيقي سببًا آخر، فهذا الإصلاح **لازم وغير كافٍ**.

### 11.4 الإصلاح — مطبَّق ومدفوع ✅

استبدال `next/font/google` بـ `next/font/local` مع حزمتَي `@fontsource` (رخصة `OFL-1.1`)، فتصل ملفات الخطوط مع `npm ci` ولا يجري البناء أي اتصال بطرف ثالث.

**تُحقِّق منه:** `npm ci` نظيف ثم بناء **مع حجب Google Fonts** → نجاح، خروج `0`، ٨ مسارات.

| | |
|---|---|
| المكان | فرع محمد — `app/fonts.ts` · `package.json` · `package-lock.json` · `design/STACK.md` §5 |
| الحالة | ✅ **مدفوع** — `38e6aaa` (الإصلاح) و`e246fb7` (تعليقات الملكية) |
| النتيجة | فحص Vercel انقلب من `failure` إلى `success` على اللقطة التالية مباشرة |

### 11.5 ✅ رابط المعاينة كان محجوبًا بـ Deployment Protection — وحُلّ

**البناء اتصلح، لكن المعاينة لا تُفتح.** فحص الرابط:

```text
GET https://dallal-cafxxfsso-rayan-saleh1.vercel.app
302 → 307 (X-Matched-Path: /api/sso) → 200
<html lang="en-US" class="… geistsans … dash">
```

`lang="en-US"` وخطوط Geist وصنف `dash` — أي **صفحة دخول Vercel**، لا تطبيق دلال. ولا أثر لكلمة «دلال» ولا `lang="ar"` في الجسم.

**السبب:** خاصية **Deployment Protection** مفعّلة على المشروع، فكل من ليس مسجّلًا في نطاق `rayan-saleh1` يرى جدار الدخول.

**لماذا يهم:** §9 من هذه الوثيقة تفترض أن «روابط المعاينة تعمل للجميع» — وهذا **غير صحيح حاليًا**. وبدونه لا يمكن تنفيذ `CP-2` (اختبار المتصفحين) ولا `V-3`، لأن المراجعين لا يفتحون المعاينة أصلًا.

**الحل — خطوة واحدة عند ريان:** إعدادات مشروع `dallal` ← **Deployment Protection** ← تعطيل الحماية على المعاينات، أو إضافة **Protection Bypass**.

> ### ✅ حُلّ — 2026-08-13
>
> **الإعداد اسمه `Vercel Authentication`، وهو مجاني على Hobby.** هذا هو الالتباس
> الذي أخّر البند: في نفس الصفحة يقع **`Password Protection`** وهو إضافة مدفوعة،
> فالقراءة الطبيعية أن الحماية كلها خلف الدفع. ليست كذلك — المطلوب تعطيل الأول
> فقط، **دون أي تغيير في الخطة**.
>
> **لوحة التحكم كانت تفشل بصمت.** الزر ينقلب، ويتفعّل `Save`، ويظهر نجاح — ثم
> تُظهر إعادة التحميل أنه **عاد مفعّلًا**، ثلاث مرات متتالية. سبب فشل النقر
> بالإحداثيات موثَّق لمن يقع فيه لاحقًا: `devicePixelRatio` يساوي `1.333`
> و`innerWidth` يساوي `2205` مقابل لقطة عرضها `1568`، ومربّع الاختيار نفسه
> `sr-only` بمقاس `1×1` بكسل داخل `<label>`.
>
> **نُفِّذ عبر واجهة Vercel البرمجية بدلًا من اللوحة:**
>
> ```text
> GET   ssoProtection: {enabled: true,  deploymentType: "all_except_custom_domains"}
> PATCH {"ssoProtection": {"enabled": false}}
> GET   ssoProtection: {enabled: false, deploymentType: null}
> ```
>
> **والقياس من الخارج — وهو الدليل الحقيقي، لا استجابة الحفظ:**
>
> ```text
> dallal-git-fix-abdulrahman-reset-origin-…vercel.app   200   (كان 302 → 307 /api/sso)
> dallal-ndmy35rwb-…vercel.app                          200
> dallal-rust.vercel.app                                200
> ```
>
> والمعاينة تقدّم التطبيق فعلًا: `<html lang="ar" dir="rtl">` وعنوان
> `دلال — منصة المزادات المباشرة` — أي نقيض اللقطة أعلاه بالضبط.
>
> **المقابل، مذكورًا لا مطمورًا:** أي شخص يملك الرابط يستطيع فتح أي معاينة.
> نُقدِّر أن هذا مقبول هنا — المستودع عام، والإنتاج عام، والمعاينات تشير إلى
> `dallal-dev` وعليه `RLS` على كل الجداول و`SELECT` فقط لـ`anon` ولا مسار
> مزايدة يتجاوز `place_bid` — لكنه **تغيّر حقيقي في الوضع الأمني**، وقرار عكسه
> يعود لمالك المشروع.

### 11.6 أثر ذلك على §10 و§8

| بند الإغلاق (§10) | الحالة |
|---|---|
| مشروع مرتبط بـ `RayanAlDwlah/dallal` — لا نسخة | ✅ **تحقّق** |
| لم يُنشأ أي مستودع ثانٍ | ✅ **تحقّق** |
| أي PR ينتج معاينة برابط مستقل | ✅ **تحقّق** — الرابط يُنتَج ويُفتح (§11.5) |
| **`main` ينشر إلى الإنتاج** | ✅ **تحقّق 2026-08-13** — بعد اندماج `PR #1` و`PR #2`، نشر `main` @ `28f54fe` رجع `success` |
| متغيّرات §8 (الإنتاج ← `dallal-prod`، المعاينة ← `dallal-dev`) | ✅ **تحقّق 2026-08-13** — انظر الجدول أدناه |
| لا `SUPABASE_SERVICE_ROLE_KEY` · لا GitHub Actions | ✅ **كلاهما مؤكَّد** — قُرئت قائمة المتغيّرات كاملة، لا استنتاجًا |

**البندان المتبقيان أُغلقا.** كلاهما كان داخل لوحة Vercel التي لا يصل إليها إلا مالك المشروع:

1. ~~**رفع Deployment Protection** عن المعاينات~~ — ✅ §11.5
2. ~~**معاينة متغيّرات البيئة**~~ — ✅ الجدول التالي

**متغيّرات البيئة — خمسة، بأسمائها وبيئاتها فقط. لم تُقرأ أي قيمة ولم تُعرَض:**

| المتغيّر | البيئة | مشروع Supabase |
|---|---|---|
| `SITE_URL` | **الإنتاج فقط** | — (لا علاقة له) |
| `NEXT_PUBLIC_SUPABASE_URL` | الإنتاج | `yfszokbunbqesigdfuwk` = **`dallal-prod`** ✅ |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | الإنتاج | — |
| `NEXT_PUBLIC_SUPABASE_URL` | المعاينة | `cjrnakdigcwnsrvtyqhy` = **`dallal-dev`** ✅ |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | المعاينة | — |

**مدخلان منفصلان لكل مفتاح، لا قيمة واحدة مشتركة** — أي أن الفصل بين البيئتين
حقيقي لا اسمي. و`SUPABASE_SERVICE_ROLE_KEY` **غير موجود**؛ هذه قراءة للقائمة
كاملة، وليست استنتاجًا من «نحن لم نضفه».

> **لماذا `SITE_URL` على الإنتاج فقط:** المعاينة تتكلم مع `dallal-dev`، فتوكن
> الاستعادة يُصدره GoTrue الخاص بـ dev بينما يهبط الرابط على تطبيق الإنتاج الذي
> يتكلم مع `dallal-prod` — فتظهر للمستخدم شاشة «رابط منتهٍ»، وهي في الحقيقة سوء
> إعداد لا انتهاء صلاحية. رصده @Dem4t في `PR #8` §4، وكان تصحيحًا لخطأ ارتُكب هنا:
> ضُبط المتغيّر أولًا على الإنتاج **والمعاينة** معًا.
>
> **`SITE_URL` مؤكَّد داخل نشرة الإنتاج العاملة، لا في اللوحة فقط.** يربط Vercel
> المتغيّرات بالنشرة **وقت البناء**، فوجودها في اللوحة لا يعني وصولها للتشغيل. تُميّز
> الدالة بين الحالتين برسالتين مختلفتين، وعنوان **غير مسجَّل** يبلغ الثانية:
>
> | الحالة | الرد |
> |---|---|
> | `SITE_URL` مفقود | «تعذّر إرسال رابط الاستعادة لأن إعداد عنوان الموقع (SITE_URL) غير مضبوط…» |
> | `SITE_URL` موجود | «إذا كان هذا البريد مسجّلًا لدينا، فسيصلك رابط…» |
>
> أُرسل `probe-no-account@example.com` — و`example.com` نطاق محجوز لدى IANA لا
> يستقبل بريدًا — فظهرت **الرسالة العامة**. أي أن `siteOrigin()` أعادت قيمة غير
> فارغة. **ولم يُرسَل بريد إلى أحد:** سوبابيس لا يرسل شيئًا لعنوان بلا حساب.

### 11.7 أثره على المصادقة

`URL Configuration` لاستعادة كلمة المرور (`AUTH-12`/`AUTH-13`) يحتاج **رابط نشر يُفتح فعلًا**، لا مجرد رابط موجود. الترتيب المحدَّث:

```text
✅ دفع إصلاح الخط
✅ معاينة تُبنى بنجاح ولها رابط
✅ اندماج PR #1 و PR #2 — main تنشر إلى الإنتاج
✅ رفع Deployment Protection            §11.5
✅ ضبط URL Configuration                @Dem4t — PR #8 §1
✅ SITE_URL على الإنتاج، ومؤكَّد بالتشغيل  §11.6
⬜ قوالب البريد
```

### 11.8 ⚠️ `V-3` — نصفه تحقّق، ونصفه الآخر **لا يمكن أن يتحقق**، وهذا يحتاج قرار الفريق

`V-3` مكتوب هكذا: «تأكيد هدف المعاينات، والتحقق من عمل إيميل استعادة كلمة المرور
**في المعاينة**».

| النصف | الحالة |
|---|---|
| هدف Supabase للمعاينات | ✅ **تحقّق** — `dallal-dev`، §11.6 |
| استعادة كلمة المرور **في المعاينة** | ❌ **غير ممكن بالتصميم الحالي** |

**السبب ليس عطلًا، بل نتيجة قرارين مسجَّلين أصلًا:**

1. المعاينة تشير إلى `dallal-dev`، والإنتاج إلى `dallal-prod` (§8) — فالتوكن
   الصادر من أحدهما لا يصلح على الآخر.
2. قائمة إعادة التوجيه في Supabase تُدخَل **بالعنوان الكامل بلا `wildcard`**
   (`PR #3` §4، ونفّذه @Dem4t في `PR #8`). ورابط كل معاينة **يتغيّر مع كل دفعة**،
   فلا يمكن إدراجها مسبقًا.

**والمخرج الوحيد هو بالضبط ما رُفض:** إدخال `https://*.vercel.app/**`، وهو يُلغي
الحماية التي وُجد `PR #8` من أجلها — ويفعل ذلك **دون أن يبدو أنه فعله**.

> **هذا ليس قرارًا تتخذه جلسة ذكاء اصطناعي** (`TEAM.md` قاعدة 16). المطروح على
> الفريق أحدهما:
>
> - **(أ)** يُعاد تعريف `V-3` بحيث تكون الاستعادة **ميزة إنتاج فقط**، ويُسجَّل ذلك
>   في `PRD` بوصفه نطاقًا مقصودًا لا نقصًا. ومن يحتاج تجربتها يشغّل محليًا مقابل
>   `dallal-dev` — وهو ما تعنيه قيمة `http://localhost:3000` المتروكة عمدًا على dev.
> - **(ب)** يُقبل الـ`wildcard` صراحةً مع تسجيل الثمن الأمني.
>
> الرأي المسجَّل هنا هو **(أ)**، ومصدره تحليل @Dem4t في `PR #8` §4. لكن `V-3`
> **يبقى مفتوحًا** حتى يقرّر الفريق، لأن إغلاقه بوصفه «متحقَّقًا» يخفي أن أحد نصفيه
> لن يُنفَّذ أبدًا.

---

## المراجع

`ARCHITECTURE.md` — §17.3 · §18 · §18.2 · §18.4 · §22 (V-3)

`GITHUB_PLAN.md` — S0-05 · S0-06 · V-3 · §11.2

`TEAM.md` — §3 · §4 · §5 (الملكيات دون تغيير) · §7

</div>
