# HANDOFF — نقطة التسليم لجلسة جديدة

> آخر تحديث: `2026-08-12`. **اقرأ هذا الملف أولاً.** ملف عمل مؤقت يُحذف قبل فتح PR على `main`.
> الجلسة السابقة `c9cb8706` ماتت بالـ limit. الحالية تكمل منها. حساب طويق قد يكمل بعدها.

---

## ١. الوضع في ثلاثة أسطر

Rayan على `feature/rayan-bidding` — **مدفوع، وفيه PR #1 مفتوح على `main`**.
الفريق **لم يعد موقوفاً**: عبدالرحمن دفع ٥ commits (S0-04 · S0-06 · S0-10 · AUTH-01)، ومحمد دفع ٢ (design system + RTL + سكافولد Next.js).
الشغل الجاري: عقود `S0-12` ✅ و `BID-02` ✅ (مُتحقَّق منها تجريبياً) و `S0-11` ✅ (مسودة تنتظر محمد).

---

## ٢. ما أُنجز في هذه الجلسة

| الملف | الحالة |
|---|---|
| `docs/contracts/S0-12-money.md` | مسترجَع. مرّ بلجنة حكّام عدائية (43.5/38.5/31). **فيه عيب في §5.1 — انظر ٤** |
| `docs/contracts/BID-02-bid-operation.md` | **مسترجَع من transcript وكيل ميت** — الـ journal ما سجّله. schema + دالة + RLS + `V-1` + `BID-20` |
| `docs/contracts/BID-02-verification.md` | **شُغّل فعلياً على PostgreSQL 17.10**: ١٧/١٧ قبول، تزامن ١٢ جولة × ٨ اتصالات، سُلّم ٦ × ٣٢ |
| `docs/contracts/S0-11-auction-record.md` | **مسودة نصف Rayan** — checkboxes في §8 ينتظر محمد يأشّر عليها |
| `docs/_wip/` | مادة خام + التناقضات الثمانية. **تُحذف قبل الـ PR** |

**درس مهم للاسترجاع:** الـ agents التي تكتب جوابها ثم تموت **لا تُسجَّل في الـ journal**. `docs/_wip/extract-workflow-output.py` صار يقرأ الـ transcripts أيضاً — وهو الذي أخرج `BID-02` (٥١,٧٢٧ حرف). شغّله دائماً:

```bash
python3 docs/_wip/extract-workflow-output.py
```

---

## ٣. الخطوة التالية بالترتيب

1. **أرسل `S0-11` لمحمد** ليؤشّر على §8. هو المخرج الوحيد المطلوب منه الآن.
2. **PR واحد يجمع RTL + صيغة المبلغ** — فرع `docs/rtl-decision`. يعدّل `PRD.md` §4.2 و `A-U10`، ويضيف متطلب RTL، ويراجع `S0-12` §7. **محمد وعبدالرحمن مراجعان إلزاميان.** عبدالرحمن لم يُبلَّغ إطلاقاً وعنده ١٤ Issue واجهات.
3. **`CLAUDE.md`** في الجذر — الثلاثة يبنون بكلود، وهو الآلية الوحيدة لتقييد الجلسات الثلاث دفعة واحدة. يجب أن يحوي: ممنوعات `S0-12` §9، وترتيب السجل بـ `id` لا `created_at`، والأربعة الممنوعة من `ARCHITECTURE §13.2a`.
4. **أضف مراجعين على PR #1** — `GITHUB_PLAN.md:264` يشترط في قبول `S0-12` أن محمد راجعه.
5. **`S0-05` Vercel — محجوب عليك أنت شخصياً.** انظر ٦.

---

## ٤. عيوب مؤكَّدة تحتاج قراراً

- **`S0-12` §5.1 — عيب حقيقي.** تعبير `outbid_race` الحرفي **لا يستطيع إنتاج المثال المشروح في §10 من نفس الوثيقة**. `BID-02` نفّذ النيّة بدل الحرف. الوثيقة مختومة `FINAL` وقاعدتها الختامية تقول إن التغيير مراجعة مستند بموافقة الثلاثة.
- **`created_at` فخ ترتيب.** قيمته `now()` = بداية المعاملة لا ترتيب القفل. الترتيب به يعرض تاريخاً **متناقصاً**. قياساً: ٢ من ١٢ مزاداً متنازعاً اختلف ترتيبهما. سلطة الترتيب `bids.id`.
- **تعارض صيغة المبلغ.** `S0-12 §7` يفرض `1250.00 SAR`؛ `money.ts` عند محمد يخرج `1,250.00 ر.س`. صيغتان = كسر `NFR-DAT-08`. **تحقّقت: `BR-33` يعرّف العملة لا الرمز، ولا `PRD` ولا `ARCHITECTURE` يذكران فاصل الآلاف أو نوع الأرقام** — فـ `ر.س` يخالف `S0-12 §7` وحده، و§7 فيه بند خروج صريح يسمّي قرار RTL سببَ تعليقه.
- **الحقل السادس** — ثلاث روايات: `GITHUB_PLAN:263` = current price · `TEAM:421` و `TEAM:372` = existence · و`TEAM:369` يناقض نفسه. الحل المقترح في `S0-11` §2.1.
- **الاستشهاد مكسور:** `S0-11` يستشهد بـ `ARCHITECTURE §10.3` وهو **Password reset** (سطر ٦٠٥).

---

## ٥. الحوكمة — نتيجة مهمة

**محمد وعبدالرحمن يقدران يدمجان على `main` بدونك.**

| الإعداد | القيمة |
|---|---|
| `enforce_admins` | ✅ `true` — لا دفع مباشر لأحد، ولا لك |
| `required_approving_review_count` | `1` |
| `require_code_owner_reviews` | ❌ `false` |
| `restrictions` (من يدفع/يدمج) | ❌ **غير مفعّلة** |
| صلاحية `m7ya505` و `Dem4t` | `write` |

محمد يفتح PR، عبدالرحمن يوافق، محمد يدمج — وأنت خارج المعادلة. و**`restrictions` غير متاحة أصلاً** لأن المستودع مملوك لـ **user** لا **organization** (`owner_type: User`؛ الـ endpoint يرجّع `404 Push restrictions not enabled`).

الحل: تحويل المستودع لـ organization (مجاني، ويعطيك `restrictions`)، أو `CODEOWNERS` + `require_code_owner_reviews` — لكن الثانية **تقفل PRs الخاصة بك** لأن GitHub يمنعك من الموافقة على PR أنت كاتبه وأنت الـ code owner الوحيد. **لم يُغيَّر أي إعداد — القرار لريان.**

---

## ٦. عليك أنت شخصياً — `S0-05` Vercel

عبدالرحمن كتب `docs/vercel-deployment.md` على فرعه وهي **رسالة موجّهة لك**. الخلاصة: ربط Vercel يتطلب `admin` على المستودع، وصلاحية `write` لا تكفي لتثبيت GitHub App.

⚠️ **مصيدة:** إدخال رابط المستودع يدوياً في Vercel يعرض مسار **Clone لا Import** — يُنشئ مستودعاً ثانياً `Dem4t/dallal` ويربط النشر بالنسخة لا بمستودع الفريق. عبدالرحمن تجنّبها بشكل صحيح.

خياران: تستورد أنت (تأكد `Import` لا `Clone`)، أو تمنحه الوصول من `github.com/settings/installations`.

---

## ٧. قيود سارية

- **المستودع عام (PUBLIC).** تحقق قبل أي دفع. آخر فحص أسرار: نظيف — الوحيد الظاهر `postgres:postgres@127.0.0.1:54322` وهي سلسلة Supabase المحلية القياسية.
- **`main` محمي** بـ `enforce_admins: true`. لا دفع مباشر ولو كنت المالك.
- **لا تُودَع أسرار أبداً** (`TEAM.md` قاعدتا 9 و 10). المفتاح المسرَّب يُدوَّر، لا يُحذف الـ commit.
- **قاعدة `TEAM.md` رقم 16** (سطر 1156): `PRD.md` مصدر الحقيقة، والمطوّر لا يخترع قرار منتج في الكود. التناقضات تُرفع للفريق لا تُحسم فردياً.
- **ممنوعات `ARCHITECTURE §13.2a`:** لا increment، لا حد أقصى، لا reserve، لا رفض للمزايد المتصدّر. إضافة أيٍّ منها **bug** لا تحسين.
- **لغات الفريق:** عبدالرحمن يوثّق بالعربية، محمد بالإنجليزية. عقود `docs/contracts/` بالإنجليزية لأن محمد مستهلكها.

---

## ٨. عطل معروف في بيئة الجلسة

إعداد الجلسة (`effort: xhigh` مع تعطيل thinking) يرفضه الـ API لكل subagent:

```
400 output_config.effort 'xhigh' is not supported when thinking is disabled
```

`opus` يفشل؛ `sonnet` و `fable` يعملان. سكربتا الـ workflow يثبّتان `const MODEL = 'fable'` لهذا. لو رجع الخطأ، السبب `effort` لا الكود.

⚠️ **الـ workflows لا تُستأنف عبر الجلسات.** بتغيير الحساب يبدأ session id جديد والاستئناف لا يعمل. لهذا **يُحفَظ كل مخرَج في git فور إنتاجه** — لا تنتظر اكتمال مرحلة.
