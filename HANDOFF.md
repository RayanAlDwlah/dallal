# HANDOFF — دلال V2 في الإنتاج

> آخر تحديث: **2026-08-16** (فجرًا). اقرأ هذا قبل أي شغل جديد.
> `CLAUDE.md §0` يشرح ليش الكود الحالي يسبق بعض الوثائق القديمة — **الكود المشحون هو الحاكم**.

## وين كل شي

| الشيء | القيمة |
|---|---|
| الإنتاج | **https://dallal-rust.vercel.app** — مشروع Vercel `dallal` (فريق `rayan-saleh1`) |
| الفرع | `delivery/v2-app` على `RayanAlDwlah/dallal` — مدفوع، والـ PR لـ main لسه ما انفتح |
| قاعدة البيانات | Supabase **`dallal-prod`** (`yfszokbunbqesigdfuwk`، Dem4t' Org) — سكيمة V2 + migration `20260816000000` (مدد مفتوحة) مطبّقة |
| **لا تلمس** | **`dallal-dev`** (`cjrnak…`) — عليه ديمو الـ MVP المجمّد (16–17 أغسطس) |
| الذكاء (برودكشن) | Gemini `gemini-3.5-flash-lite` عبر endpoint متوافق مع OpenAI — env على Vercel، المفتاح خادمي فقط |
| الذكاء (محلي) | نفس Gemini في `.env.local`؛ البديل بدون نت: LM Studio `google/gemma-4-e4b` (معلّق في نفس الملف) |
| حدود Gemini المجانية | flash-lite: 15 طلب/دقيقة، **500 طلب/يوم** (وflash العادي 20/يوم فقط — لا تبدّل له) |

## حسابات التجربة (على dallal-prod، الدخول بدون تأكيد إيميل)

`Dallal-2026!` هي كلمة المرور للكل: `rayanaldwlah+dallal@` (ريان — بائع الرولكس)،
`+meshal@` (مشعل)، `+saud@` (سعود). ملف CSV جاهز للتجربة: `public/sample-lots.csv`.

## وش المتحقق منه (مقاسًا، مو منقولًا)

تسجيل → «الشريطي يكتب لك» قرأ صورة رولكس وكتب الإعلان وصنّفه → نشر برفع صورة →
اقتراح السعر اختفى تحت 3 مقارنات → مزايدة بالزر (أول مزايدة = سعر البداية) →
بحث «ابغا رولكس» يلقى الرولكس (فلتر الحشو) → الشريطي يحوّل «كامري 2020 تحت 60 ألف»
لشرائح قابلة للحذف → استيراد CSV ركّب 3 قطع بتصنيفات مطابقة → «بدون مدة» تنحفظ
وتظهر → محرر الصور يدوّر ويحفظ ويعرض «رجّع الأصلية» → **إشعار «زايد عليك» وصل
منبثقًا لمتصفحٍ على الرئيسية لحظة المزايدة** (realtime، مع زر «زايد بـ»).

## طبقة الشريطي — الحدود اللي لا تنكسر

`design-system/previews/ai.html` هي العقيدة: الذكاء على مسار الإدخال والعرض فقط.
ما يزايد، ما يقبل/يرفض مزايدة، ما ينهي مزادًا، ما يشوف بريدًا ولا معرّفًا داخليًا،
**وما يُنتج مبلغًا أبدًا** — اقتراح السعر SQL/BigInt، وأرقام البحث والـ CSV استخراج
من نص المستخدم/ملفه يُعاد التحقق منه بـ `MONEY_RE`. بدون إعداد `AI_*` كل شي يختفي
بهدوء والمنتج يشتغل. **ما فيه شات عائم** — التصميم نفسه يمنع «أيقونة سادسة».

## المتبقي / المؤجل

1. **PR لـ main**: https://github.com/RayanAlDwlah/dallal/pull/new/delivery/v2-app
2. **«يصلّح الصور» بالذكاء** (خلفية نظيفة/إزالة انعكاس): يحتاج مزوّد image-edit —
   الموجود الآن محرر يدوي (تدوير/إضاءة/قص) والبنية جاهزة للتوسعة.
3. **توكن Supabase** `dallal-v2-ship` بحساب ريان: يُحذف بعد الاستقرار (ينتهي 14 سبتمبر).
4. نوع المضيف (فرد/معرض) على بطاقة الجلسة — يحتاج عمود جديد؛ مؤجل عمدًا.
5. مشروع عبدالرحمن الجانبي (`dallal-v2` / `dymm…`) صار بريفيو خاصًا فيه — منصتنا مفصولة عنه.

## للتشغيل محليًا

```bash
npm install && npm run dev   # .env.local موجود ومضبوط على dallal-prod
```
