# HANDOFF — نقطة التسليم لجلسة جديدة

> اكتُب في `2026-08-12` من جلسة `c9cb8706-0888-429d-a4a2-8cb2b7afcbd0` قبل انتهاء الـ limit.
> **اقرأ هذا الملف أولاً.** هو ليس جزءاً من مستندات المشروع — ملف عمل مؤقت، غير مرفوع.

---

## ١. الوضع الحالي في سطرين

Rayan (`@RayanAlDwlah`) يشتغل على Sprint 0. محمد (`@m7ya505`) **موقوف** ينتظر نصف Rayan من عقد `S0-11`.
الشغل الجاري: إنتاج عقدين — `S0-12` (تمثيل المبلغ) و `S0-11` (عقد سجل المزاد) — عشان يفكّان الانسداد.

**الفرع الحالي:** `feature/rayan-bidding` — أُنشئ محلياً، **غير مدفوع**، ما فيه commits جديدة.
**`main` محمي** بـ `enforce_admins: true` — أي تغيير يمر عبر PR بمراجعة واحدة. هذا ينطبق على Rayan نفسه.
**المستودع `RayanAlDwlah/dallal` عام (PUBLIC).** تأكد قبل رفع أي شيء.

---

## ٢. الملفات الموجودة على القرص الآن (كلها غير مرفوعة — `git status` يظهر `?? docs/`)

| المسار | ما هو | الحالة |
|---|---|---|
| `docs/contracts/S0-12-money.md` | **عقد تمثيل المبلغ SAR — كامل، 28,520 حرف** | ✅ ناتج نهائي، لم يُراجَع بشرياً بعد |
| `docs/_wip/s012-structured.json` | التصاميم الثلاثة + أحكام اللجنة على كل واحد | مادة خام |
| `docs/_wip/s011-structured.json` | استخراج الحقائق من أربع جهات + **التناقضات الثمانية** | مادة خام |
| `docs/_wip/extract-workflow-output.py` | **سكربت يعيد استخراج أي agent خلص بعد كتابة هذا الملف** | شغّله أولاً |

**أول أمر تشغّله في الجلسة الجديدة:**

```bash
python3 docs/_wip/extract-workflow-output.py
```

قد يكون خلّص agents إضافية بعد كتابة هذا الملف. السكربت آمن للتشغيل المتكرر.

---

## ٣. أهم اكتشاف — كسر حقيقي في المستندات

مُتحقَّق منه بـ `grep`، ليس انطباعاً. وكيل صيد التناقضات أكّده ووسّعه.

### CRITICAL ١ — لا توجد طريقة تفرّق بين "ما فيه مزايدات" و"فيه مزايدات"

- `BR-28` يفرض مقارنتين مختلفتين: لا مزايدات ← `amount >= starting_price` (شامل). فيه مزايدات ← `amount > current_price` (أكبر تماماً).
- `SC-55`: أول مزايدة تساوي سعر البداية بالضبط ← **تُقبَل**.
- `SC-56`: بعدها مباشرة نفس المبلغ ← **يُرفَض**، و`+0.01` ← يُقبَل.
- لكن `TEAM.md:595` (M-09) يقول عند النشر: `current_price = starting_price`.

**النتيجة:** لو قارنت الدالة `amount > current_price` فقط، مزايدة بـ `100` على سعر بداية `100` تُرفَض — و`SC-55` ينكسر. **ولا مستند من الأربعة يعيّن حقلاً يحمل حقيقة "هل يوجد مزايدة".** ولا يعيّن من يكتبه.

وأسوأ: `GITHUB_PLAN.md:263` (acceptance criteria حق `S0-11`) ينص أن Rayan يكتب «current price on each accepted bid **and nothing else**» — أي أن نص العقد نفسه **يمنع** تخزين هذا الحقل. هذا تعارض يحتاج قرار فريق، لا قرار Rayan (قاعدة `TEAM.md` رقم 16).

### CRITICAL ٢ — الحقل السادس مختلف في ثلاثة مستندات

| المصدر | الحقل السادس |
|---|---|
| `GITHUB_PLAN.md:263` | `current price` |
| `TEAM.md:421` (§10.3) | `whether the auction exists` |
| `ARCHITECTURE.md` | نسخة ثالثة، من خمسة حقول |

و`TEAM.md` يناقض نفسه داخل الجدول الواحد: سطر `372` يعدّد الحقول بدون current price، وسطر `369` فوقه يقول إن `BR-03` يحتاج «the current price and starting price».

### والستة الباقية (تفاصيلها في `docs/_wip/s011-structured.json`)

- **MAJOR** — من يهيّئ `current_price` عند الإنشاء؟ `ARCHITECTURE` يقول «Only the bid operation» بلا استثناء للإنشاء، بينما `PRD`/`TEAM`/`GITHUB_PLAN` يطلبون من محمد ضبطه لسعر البداية.
- **MAJOR** — `status` كمدخل للتحقق: `TEAM`/`GITHUB_PLAN` يجمّدانه ضمن «الحقول التي يعتمد عليها التحقق»، بينما `PRD LC-03` **يمنع** اشتقاق أهلية المزايدة من علم الحالة المخزّن — ولا خطوة واحدة في عملية المزايدة تستهلكه فعلاً.
- **MAJOR** — محتوى حمولة الـ realtime: `PRD` يطلب أسماء العرض ويمنع المعرّفات الداخلية الزائدة، لكن آلية `ARCHITECTURE` تبثّ تغييرات الصفوف كما هي، وادعاء «satisfied structurally» يغطي الإيميلات فقط.
- **MINOR** — جملة صلاحيات الكتابة في `TEAM.md` تُسقط كتابة Rayan لـ current price، وتناقض «feedback path» بعدها بثلاثة أسطر.
- **MINOR** — `S0-11` يستشهد بـ «ARCHITECTURE §10.3» وهو **قسم استعادة كلمة المرور**، لا عقد سجل المزاد. استشهاد خاطئ.
- **MINOR** — من يزوّد سعر صفحة القوائم؟ `TEAM.md` (M-10) يقول من Rayan مشتقاً من التاريخ، و`ARCHITECTURE §9.4` يقول محمد يقرأ الحقل مباشرة بلا تجميع.

---

## ٤. ما كان طائراً وضاع

الـ workflows تُستأنف **داخل نفس الجلسة فقط**. بتغيير الحساب تبدأ جلسة جديدة، فالاستئناف **لا يعمل**. الـ agents المكتملة محفوظة في الـ journals ويستخرجها السكربت أعلاه.

| Run ID | المهمة | خلص | ضاع |
|---|---|---|---|
| `wf_47dfc3cc-3f1` | S0-12 المبلغ + عملية المزايدة | 7/11 — التصاميم والأحكام و**العقد النهائي** | `bid:design` (SQL عملية المزايدة + `V-1` + اختبار `BID-20`)، ثم 4 عدسات عدائية والناقد |
| `wf_92526581-c39` | S0-11 عقد سجل المزاد | 7/~13 — الاستخراج و**التناقضات الثمانية** | تصاميم مشكلة التمييز، اللجنة، المسودة، العدسات، الناقد |

سكربتا الـ workflow محفوظان ويمكن إعادة تشغيلهما كما هما:

```
~/.claude/projects/-Users-ry7vv-Documents-Coding-Project-dllal/c9cb8706-0888-429d-a4a2-8cb2b7afcbd0/workflows/scripts/
  dalal-s012-and-bid-operation-wf_8614ac89-6f7.js
  dalal-s011-auction-record-contract-wf_92526581-c39.js
```

⚠️ **عطل معروف:** إعداد الجلسة (`effort: xhigh` مع تعطيل thinking) يرفضه الـ API لكل subagent. `opus` يفشل؛ `sonnet` و `fable` يعملان. السكربتان يثبّتان `const MODEL = 'fable'` لهذا السبب. لو رجع نفس الخطأ في الجلسة الجديدة، السبب هو `effort` لا الكود.

---

## ٥. الخطوة التالية بالترتيب

1. **شغّل سكربت الاستخراج** — قد يكون خلّص شيء إضافي.
2. **راجع `docs/contracts/S0-12-money.md`** — ناتج لم يمر بمراجعة عدائية (تلك المرحلة ضاعت). تحقق تحديداً من: `NFR-DAT-05` (عشري دقيق، لا floating point)، `BR-21` (لا حد أقصى — هل النوع المختار يفرض سقفاً ضمنياً؟)، أثر `SC-56` عند فرق `0.01`.
3. **أعد تشغيل workflow الـ `S0-11`** — هو الذي يفكّ انسداد محمد. المخرج المطلوب: مستند يعطيه Rayan لمحمد فيه checkboxes يؤشّر عليها، وقسم صريح «ما لا أطلبه» يعدّد الأربعة الممنوعة في `ARCHITECTURE §13.2a` حتى لا يضيف كلود حق محمد عمود `reserve_price` أو `bid_increment` مساعدةً.
4. **التناقضات الثمانية تحتاج قرار فريق، لا قرار Rayan.** `TEAM.md` قاعدة 16: `PRD.md` هو مصدر الحقيقة، والمطوّر لا يخترع قرارات منتج في الكود. اعرضها كـ PR على المستندات باسم كلا الزميلين كمراجعين مطلوبين.
5. **افتح PR** على `feature/rayan-bidding` بمحمد كمراجع مطلوب — `GITHUB_PLAN.md:264` يشترط في acceptance criteria حق `S0-12` أن **محمد راجعه وأكّده**.

---

## ٦. قيود سارية يجب ألا تُخرق

- **المستودع عام.** تأكد من الرؤية قبل رفع أي شيء. `.env.example` فيه أسماء placeholder فقط — تحقق منه قبل أي دفع.
- **`main` محمي** بـ `enforce_admins: true`. لا دفع مباشر، ولو كنت المالك.
- **لا تُودَع أسرار أبداً** (`TEAM.md` قاعدتا 9 و 10). المفتاح المسرَّب يُدوَّر، لا يُحذف الـ commit.
- **ممنوعات المنتج من `PRD.md` v3.0:** لا دفع/شراء/تواصل/شحن، لا إلغاء أو تعديل مزاد، لا مسودة محفوظة، لا واجهة إدارية، لا إيميلات مكشوفة في أي مكان بما فيه حمولات الـ realtime، لا صور شخصية أو ملفات عامة، لا bid increment، لا حد أقصى، لا reserve، لا anti-sniping.
- **قرار RTL غير محسوم في المستندات.** Rayan قرّر أن الواجهة عربية RTL، و`PRD.md` v3.0 ما زال يناقض ذلك في ثلاثة مواضع: §4.2، §19.7، والافتراض `A-U10`. محمد أُبلغ شفهياً؛ **عبدالرحمن لم يُبلَّغ** وهو يملك 14 Issue في الواجهة.
- **مشكلة قائمة:** مصادقة SSH عند عبدالرحمن معطّلة — رفع عبر واجهة GitHub («Add files via upload»، commit `ba92199`).

---

## ٧. معروضات لم يُبتّ فيها بعد

- `S0-03` — إنشاء 22 label و 5 milestones و PR template و 84 Issue على GitHub.
- PR باسم `docs/rtl-decision` يعدّل `PRD.md` §4.2 و `A-U10` ويضيف متطلب RTL إيجابياً.
- `CLAUDE.md` في جذر المستودع — **الأعلى أثراً**: الثلاثة يبنون بكلود، وهذا الملف الآلية الوحيدة التي تقيّد الجلسات الثلاث دفعة واحدة.
- Issue جديد لبنية اختبار حقيقية لـ `BID-20` (`GITHUB_PLAN.md:463` يسمّيه «the single most important test in the project» ويطلب تشغيله متكرراً — بينما GitHub Actions ممنوع صراحةً في ستة مواضع، فلا توجد آلية تشغيل متكرر).
- تقديم `S0-04` في الترتيب — يحجب `V-1` و `V-2` معاً.
